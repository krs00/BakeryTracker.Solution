@* I didn't make it that far with commenting my code :( *@
@* It is a lot of code so I can't comment everything it would be to slow!! *@
@{
  Layout = "_Layout"; 

  @* Retrives all data associated with the "vendor" key
  Stores in "vendor" variable
  It is so I can access the actual model data
  because before I think I was trying to get it from the dictionary *@
  Vendor vendor = Model["vendor"];

  Order order = Model["orders"];
}

@using BakeryTracker.Models;

<h1>🍞 😊 Pierre's Bakery 😊 🍞 </h1>
<h2>Vendor Information</h2> 
<br>
<h4>@vendor.Name</h4>
<br>
<p>@vendor.Description</p>

<br>
<br>
<br>



<br>
<br>
<br>
<br>
@* probably won't add CSS to this one, so sorry for the line breaks
just trying to make it look semi-nice *@ 
<h4><a href="/">Add order</a></h4>
<br>
<h4><a href="/vendor">Back to vendor list</a></h4>
<h4><a href="/">Back to home page</a></h4>
