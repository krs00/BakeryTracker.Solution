@* I didn't make it that far with commenting my code :( *@
@* It is a lot of code so I can't comment everything it would be to slow!! *@

@{
  Layout = "_Layout";

}

@using BakeryTracker.Models;

<h1>🍞 😊 Pierre's Bakery 😊 🍞 </h1>
<h2>Vendor Orders</h2> 
<br>
<h4> Here are all the orders for the vendor <span style="color:red">@Model["vendor"].Name</span></h4> 

 <ol>
    @foreach (Order order in @Model["orders"])
    {
      <li><a href='/vendors/@Model["vendor"].Id/orders/@order.Id'>@order.Description</a></li>
    }
  </ol> 
</div>


<br>
<br>
@* probably won't add CSS to this one, so sorry for the line breaks
just trying to make it look semi-nice *@
<h4><a href='/vendors/@Model["vendor"].Id/orders/new'>Add order</a></h4>
<br>
<h4><a href="/vendors">Back to vendor list</a></h4>
<h4><a href="/">Back to home page</a></h4>
